# ============ Koenig Solutions — Single WhatsApp Brand Account ============
#
# All WhatsApp messages are sent from ONE Koenig Solutions Business number.
# CSM assignments are tracked for reporting but messages come from the brand.
#
# SETUP STEPS:
# 1. Go to Meta Business Manager: https://business.facebook.com/
# 2. Create a WhatsApp Business App (or use existing)
# 3. Go to: Meta for Developers > Your App > WhatsApp > API Setup
# 4. Register & verify your Koenig Solutions phone number
# 5. Get your credentials:
#
# WHATSAPP_ACCESS_TOKEN: Permanent System User token
#   - Business Settings > System Users > Add > Generate Token
#   - App: your WhatsApp app
#   - Permissions: whatsapp_business_messaging, whatsapp_business_management
#   - ⚠️ Use PERMANENT token, NOT the temporary test token (expires in 24h)
#
# WHATSAPP_PHONE_NUMBER_ID: Your Koenig WhatsApp number's ID
#   - Found on WhatsApp > API Setup page (numeric ID, NOT the phone number)
#   - Example: 123456789012345
#
# WHATSAPP_BUSINESS_ACCOUNT_ID: Your WABA ID
#   - Found on WhatsApp > API Setup page
#
WHATSAPP_ACCESS_TOKEN=your_permanent_system_user_token
WHATSAPP_PHONE_NUMBER_ID=your_koenig_phone_number_id
WHATSAPP_VERIFY_TOKEN=sales_dashboard_verify
WHATSAPP_BUSINESS_ACCOUNT_ID=your_waba_id

# Brand name shown in dashboard (default: Koenig Solutions)
WA_BRAND_NAME=Koenig Solutions

# WhatsApp API version (default: v21.0)
# WA_API_VERSION=v21.0

# Server
PORT=8080

# LinkedIn Ads API (proxy)
LINKEDIN_API_URL=https://linkedin-ads-dashboard.vercel.app/api/linkedin/leads?accountId=517988166&limit=500

# ============ Email / SMTP Configuration ============
# Set EMAIL_ENABLED=true to send real emails (false = mock/dry-run mode)
EMAIL_ENABLED=false

# SMTP Server Settings
# Microsoft 365:  smtp.office365.com / 587 / false
# Gmail:          smtp.gmail.com / 587 / false
# SendGrid SMTP:  smtp.sendgrid.net / 587 / false (user=apikey, pass=SG.xxx)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Credentials — the service account that authenticates with the mail server
# For Microsoft 365: use a shared mailbox or service account email + app password
# For Gmail: use email + App Password (enable 2FA first)
# For SendGrid: user=apikey, pass=your_sendgrid_api_key
SMTP_USER=noreply@koenig-solutions.com
SMTP_PASS=your_smtp_password_or_app_password

# Default sender (used when no CSM is assigned)
EMAIL_DEFAULT_FROM=noreply@koenig-solutions.com
EMAIL_DEFAULT_FROM_NAME=Koenig Solutions

# Optional reply-to address
EMAIL_REPLY_TO=
